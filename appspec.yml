version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/development/lip-api-server

permissions:
  - object: /home/ubuntu/development/lip-api-server
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: scripts/after-development-deploy.sh
      timeout: 300
      runas: ubuntu
